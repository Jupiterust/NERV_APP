# Modbus 点表 —— 决赛现场速查手册

> 生成于 2026-08-14，跟 `TF卡_决赛预制代码包.md` 是同一套写法：现场拿到官方要求后，**只改
> `Function/modbus_data_map.h` 里的 `#define`，不动 `.c`、不动 `Protocol` 层**。
> Modbus 协议栈本身（RTU/ASCII 判帧、CRC/LRC 校验、功能码分发、寄存器缓冲区）是这次会话
> 之前就已经写好、跑过的老代码，不是这次新加的——跟 `file_service.c`「从没编译过」的风险等级不一样，
> 这份手册要盯的主要是「点表内容对不对」，不是「协议栈能不能跑」。
> 相关：`初赛_功能测试报文清单.md`（自定义协议报文表，写法可以照抄）、
> `文档/1 2026年CIMC工业嵌入式系统开发 初赛 赛题.pdf`、
> `文档/2026年CIMC工业嵌入式系统开发方向（试）全国总决赛 样题.pdf`。

## 落地状态一览

| 项 | 状态 | 位置 |
|---|---|---|
| RTU / ASCII 判帧、CRC16 / LRC 校验 | ✅ 已有，老代码 | `Protocol/Protocol_Router.c` |
| 功能码 01/02/03/04/05/06/07/08/15/16/17/23 | ✅ 已有，其中 07/08 是自己补的（vendor 没实现） | 同上 |
| 默认点表（CH0/CH1、决赛三路、告警、TF卡开关…） | ✅ 已有 | `Function/modbus_data_map.h/.c` |
| 与自定义协议共存、总线仲裁 | ✅ 已有 | `Protocol_Router.c` 的 `Protocol_ModbusMasterActive()` |
| `eide.yml` / `test.uvprojx` 两份工程文件同步 | ✅ 已核对一致 | `Project/.eide/eide.yml`、`Project/test.uvprojx` |
| **真实上位机软件（Modbus Poll 等）连线实测** | ⬜ **没做，必须你来做** | 见下面第九节自检清单 |

**没有做过的**：拿真的 Modbus 主站软件连过这块板子——协议栈本身是成熟代码，但"默认点表 + 这块具体硬件"这个组合还没有被实际验证过。第一件事是接上 Modbus Poll 读一次 CH0 当前值。

---

## 一、题目里关于 Modbus 的原文（全部，就这么多）

初赛赛题**完全没有提 Modbus**。Modbus 是决赛样题新加的三项要求之一：

> 赛题任务在初赛的要求基础之上，涉及要求包括：
> ➢ 能实现TF卡的文件读写功能。
> ➢ 能够利用AD3344 ADC芯片（SPI通讯协议）设计4~20mA电流、0~10V电压采样电路。
> ➢ **能实现modbus通讯功能。**
>
> 嵌入式程序开发：……
> ➢ **利用Free Modbus库完成Modbus从站开发。**

**就这两句话，没有点表、没有地址、没有功能码列表**——跟 TF 卡是一个套路："详细的程序设计要求"要等决赛现场才发。这份手册存在的意义，就是让官方地址表一发下来，能在几分钟内把默认点表改成官方要求的样子，而不是临场重新设计整套映射。

---

## 二、现场 5 分钟改指南 ⭐拿到题先看这页

对照官方给的点表，大概率会遇到下面几种情况——`modbus_data_map.h` 自己的【1】现场速查段落写得比这里还细，这里给的是浓缩版对照表：

| 官方要求长这样 | 改哪里 | 例子 |
|---|---|---|
| 某个量的地址跟默认表不一样（最常见） | 【5】~【8】里对应那一行 | `#define MB_IREG_CH0_VALUE 10` |
| 整表往后平移了一截 | 【3】的地址基址，一次搞定一整类 | `#define MB_HREG_ADDR_BASE 100` |
| 要放大 N 倍的整数，不要 float | 【4】的倍数，地址挪到官方要求的位置 | `#define MB_SCALE_CH0 100.0f` |
| 读出来数值离谱 / 小数点乱跳 | 【4】的 `MB_FLOAT_BYTE_ORDER`，4 选 1 试 | 0=ABCD(默认) / 1=CDAB / 2=BADC / 3=DCBA |
| 从站地址钉死成某个数 | 【2】的 `MB_FORCE_SLAVE_ADDR` | `#define MB_FORCE_SLAVE_ADDR 5` |
| 官方波特率编码值不一样 | 【2】那 4 个 `MB_BAUD_CODE_*` | — |
| 官方点表有本机现在没有的量 | 见下面「新增一个点」三步 | — |
| 官方地址号很大，编译报错 | 【3】的 `MB_*_COUNT` 调大 | `#define MB_HREG_COUNT 512` |
| 要求强制只认 RTU 或只认 ASCII | 【2】的 `MB_DEFAULT_PROTOCOL_MODE` | 2=RTU / 3=ASCII |

**地址填错或超容量会在编译期报错**（"size of array is negative"，`modbus_data_map.c` 顶部一串 `MB_CHK(...)`），不会等到烧板子才发现。

### 新增一个官方要求但本机没有的点，三步

```c
// 1) modbus_data_map.h【5】~【8】里加一行地址定义
#define MB_IREG_MY_NEW      50      // float : 我的新量

// 2) modbus_data_map.c 顶部加一行编译期越界检查
MB_CHK(ireg_my_new, MB_IREG_MY_NEW, 2, REG_INPUT_SIZE);
//     占几个寄存器：float/u32 填2，u16/线圈/离散填1
//     表容量宏：输入寄存器REG_INPUT_SIZE / 保持寄存器REG_HOLD_SIZE
//              / 线圈REG_COILS_SIZE / 离散输入REG_DISC_SIZE

// 3) modbus_data_map.c 的 Modbus_SyncRegsFromDevice() 里加一行搬运
reg_write_float(REG_INPUT_BUF, MB_IREG_MY_NEW, 我的变量);
// 整数：REG_INPUT_BUF[MB_IREG_MY_NEW] = 我的变量;
// u32：reg_write_u32(...)　缩放整数：reg_write_scaled(...)
```
**如果这个点是可写的**（进保持寄存器或线圈），第 3 步之外还要在 `Modbus_ApplyHoldingRegs()` / `Modbus_ApplyCoils()` 里加一行反向搬运——那两个函数里每一项都是现成例子，照抄就行。**别漏这一步**，只做 Sync 不做 Apply，主站写进去的新值会在下次同步时被旧值覆盖回去，表现成"写了但没生效"。

---

## 三、地址换算——最容易被坑的一点

Modbus 有两套地址写法，混着用的表现是**整体差1**或**整体差一大截**：

```
线上地址(协议地址, Base 0)：报文里真正传输的数字，从0开始
PLC地址  (Base 1, 5位)     ：40001 / 30001 / 10001 / 00001 这种写法

换算：保持寄存器 40001 ↔ 线上 0   （减 40001）
      输入寄存器 30001 ↔ 线上 0   （减 30001）
      离散输入   10001 ↔ 线上 0   （减 10001）
      线圈       00001 ↔ 线上 0   （减     1）
```
`modbus_data_map.h` 里所有 `#define` 填的都是**线上地址**。Modbus Poll 的 Address 栏默认也显示线上地址，勾了 `Display → PLC Addresses (Base 1)` 才会变成5位写法——**官方给的点表如果是5位数写法，先按上表换算成线上地址再填进代码，不要直接把5位数字抄进 `#define`**。

四类数据区的读写权限是协议规定死的，不是本工程的限制：

| 数据区 | 位宽 | 权限 | 读功能码 | 写功能码 | 本工程放什么 |
|---|---|---|---|---|---|
| 线圈 | 1位 | 读写 | FC01 | FC05/FC15 | 开关（启停自动上报…） |
| 离散输入 | 1位 | 只读 | FC02 | — | 状态位（超限、断线…） |
| 保持寄存器 | 16位 | 读写 | FC03 | FC06/FC16 | 参数（变比、阈值、DAC…） |
| 输入寄存器 | 16位 | 只读 | FC04 | — | 测量值（电压、电流…） |

官方点表里"可写"的量必须放保持寄存器或线圈，"只读"的量放输入寄存器或离散输入——**判断标准是"这个量该不该让主站写"和"是数值还是0/1"两条交叉判断，不是简单的"数据/开关"二分**。放错区的后果是主站按它的功能码读不到/写不进。

一个寄存器16位，float 和32位整数占2个连续寄存器，两个寄存器谁在前谁在后就是【4】的 `MB_FLOAT_BYTE_ORDER` 决定的事。

---

## 四、`Function/modbus_data_map.h` —— 现场只改这个文件

全部是 `#define`，没有逻辑代码，目录（按现场用到的先后顺序）：

```
【0】 Modbus基础       不熟悉Modbus先看这段（帧格式、异常码含义）
【1】 现场速查         拿到官方点表照着做，7个场景（本手册第二节的详细版）
【2】 通信身份         从站地址、协议模式、波特率编码表
【3】 地址基准         整表平移、表容量
【4】 数据编码         float字序、缩放倍数
【5】 点表本体A        输入寄存器 FC04（只读，测量值）
【6】 点表本体B        保持寄存器 FC03/06/16（可读写，参数）
【7】 点表本体C        线圈 FC01/05/15（可读写，开关）
【8】 点表本体D        离散输入 FC02（只读，状态位）
【9】 行为参数         命令字、DAC上限、上报周期、TF卡文件名等
【10】排错速查         通不了/数值不对时看这段
【11】默认点表总表     给主站那边看的（下面第七节摘出来了）
【12】对外接口         不用改
```
【2】【3】不受点表总开关影响——就算把最上面那行 `#define MB_POINT_MAP_ENABLE` 注释掉（整个点表关掉，退化成"能收发但读写的是4个空数组"的裸壳），从站地址、协议模式、地址基址、表容量这几个仍然生效，因为 `port.c`/`Protocol_Router.c` 依赖它们。

---

## 五、`Function/modbus_data_map.c` —— 结构和调用时机

不用改，除非在做第二节"新增一个点"那三步。理解它的关键是**没有周期性刷新寄存器这回事**，四个时机由 `Protocol_Router.c` 的 `Modbus_DispatchPDU()` 精确调度：

```
分发前          Modbus_SyncRegsFromDevice()   设备数据 → 寄存器，保证主站读到当前值
写保持寄存器后   Modbus_ApplyHoldingRegs()     寄存器 → 设备，参数落地并存flash
写线圈后        Modbus_ApplyCoils()           寄存器 → 设备，开关量落地
应答发完后      Modbus_ExecPendingActions()   波特率切换/重启/睡眠/OTA这类会打断通信的动作
```
`Modbus_ApplyHoldingRegs()` 里几乎每一项写 flash 前都先判断"值真的变了吗"（`float_changed()`，位比较不用 `!=`，避免空白flash读出来是NaN导致的误判）——这是防止主站轮询式地重复写同一个值，把没有磨损均衡的flash扇区写坏。改点表新增可写的浮点参数时，照这个模式抄，别漏掉变化检测。

---

## 六、测试报文（RTU，从站地址=1，CRC16已算好）

用 Modbus Poll 这类工具的话直接填地址+功能码就行，CRC它自己算，不需要手拼。下面这几条是给用原始串口工具/写测试脚本时用的，CRC16-Modbus 是 poly `0xA001` 反射、初值 `0xFFFF`，**追加时低字节在前**（这一点跟自定义协议的CRC16是大端反过来的，别搞混）。

| 测试项 | 报文 | 期望应答 |
|---|---|---|
| 读CH0当前值（输入寄存器0，float占2个） | `01 04 00 00 00 02 71 CB` | `01 04 04 <4字节float> <CRC>` |
| 读CH0变比（保持寄存器0，float占2个） | `01 03 00 00 00 02 C4 0B` | `01 03 04 <4字节float> <CRC>` |
| 写CH0变比=2.0（保持寄存器0，2个，0x40000000） | `01 10 00 00 00 02 04 40 00 00 00 E6 6F` | `01 10 00 00 00 02 <CRC>` |
| 开自动上报（线圈0=ON） | `01 05 00 00 FF 00 8C 3A` | 原样回显 |
| 关自动上报（线圈0=OFF） | `01 05 00 00 00 00 CD CA` | 原样回显 |
| 读CH0超限状态（离散输入0） | `01 02 00 00 00 01 B9 CA` | `01 02 01 <状态字节> <CRC>` |
| 读异常状态（FC07，无参数） | `01 07 41 E2` | `01 07 <状态字节> <CRC>` |

第一条（`01040000000271CB`）跟 `modbus_data_map.h`【0】④ 自己举的例子完全一致，可以当"我的CRC算法/工具没配错"的锚点自检——如果算出来的不是这个，先怀疑CRC字节序或从站地址填的不对，不要往下继续测。

**建议测试顺序**：FC04读CH0（确认链路通）→ FC03读CH0变比（确认保持寄存器区能读）→ FC16写变比再FC03读回（验证写生效）→ 断电重启再FC03读一次（验证flash持久化）→ FC05开自动上报，同时观察RS485总线（验证`Protocol_ModbusMasterActive()`确实压住了自定义协议的主动上报，不会跟Modbus轮询打架）→ FC07读异常状态。

---

## 七、默认点表总表（当前生效的地址，给主站那边核对用）

**输入寄存器 FC04（只读）**

| 线上地址 | 内容 | 类型 |
|---|---|---|
| 0 | CH0当前值(已乘变比) | float, 2reg |
| 2 | CH1当前值(已乘变比) | float, 2reg |
| 4 | CH2温度(PT100遗留) | float, 2reg |
| 6 | CH0原始值(未乘变比) | float, 2reg |
| 8 | CH1原始值(未乘变比) | float, 2reg |
| 10~12 | CH0/CH1/CH2缩放整数镜像 | u16 × 3 |
| 13~14 | 秒级时间戳 | u32, 2reg |
| 15~16 | 固件版本号 | u32, 2reg |
| 17 | 自动上报标志 | u16 |
| 18 | 告警模式 | u16 |
| 19 | 上报间隔码 | u16 |
| 20 | 波特率码 | u16 |
| 21 | 自定义协议设备ID | u16 |
| 22 | Modbus从站地址 | u16 |
| 23 | 协议模式 | u16 |
| 24 | 告警记录条数 | u16 |
| 25 | TF卡就绪(最近一次写是否成功) | u16 |
| 26~27 | i0_current(决赛CH0电流mA) | float, 2reg |
| 28~29 | v0_voltage(决赛CH1电压V) | float, 2reg |
| 30~31 | v1_voltage(决赛CH2电压V) | float, 2reg |
| 32 | i0_broken(断线状态) | u16 |
| 告警记录读窗口、i0/v0/v1缩放镜像等 | 见 `modbus_data_map.h`【5】原文 | — |

**保持寄存器 FC03/06/16（可读写）**

| 线上地址 | 内容 | 类型 |
|---|---|---|
| 0~1 | CH0变比 | float, 2reg |
| 2~3 | CH1变比 | float, 2reg |
| 4~5 | CH0阈值 | float, 2reg |
| 6~7 | CH1阈值 | float, 2reg |
| DAC码值、上报间隔、告警模式、设备ID、波特率码、从站地址、设置时间、告警记录序号、命令寄存器 | 见 `modbus_data_map.h`【6】原文 | — |

**线圈 FC01/05/15（可读写）**：自动上报开关(0)、告警主动上报开关(1)、清除告警(2,脉冲型)、写入TF卡(3,脉冲型)。
**离散输入 FC02（只读）**：CH0超限(0)、CH1超限(1)、自动上报中(2)、告警记录非空(3)、TF卡就绪(4)、CH0断线(5)。

以上是当前默认值——**改了`modbus_data_map.h`里的地址之后记得回来同步这张表**，不然这张表会跟代码脱节，反而误导人（这也是`modbus_data_map.h`【11】自己写的提醒）。

---

## 八、现场应变剧本

| 官方要求 | 怎么应对 |
|---|---|
| 只考FC03/04，不考线圈/离散输入 | 不用删代码，多余的点主站不读就等于不存在 |
| 官方要求Modbus RTU/ASCII二选一，不许自动嗅探 | `MB_DEFAULT_PROTOCOL_MODE` 改成 2 或 3 |
| 官方波特率跟当前默认(19200)不一样 | 改 `PARAM_DEFAULT_BAUDRATE`（`param_handle.h`），或现场用自定义协议/Modbus命令改了以后它会自动存flash |
| 官方从站地址跟默认(1)不一样 | `MB_FORCE_SLAVE_ADDR` |
| 官方要求Modbus和TF卡联动（比如写线圈触发存卡） | 已有现成例子：`MB_COIL_LOG_TO_SD`，抄这个模式 |
| 官方要求某个功能码本机没实现（比如FC22/24这类少见的） | `Protocol_Router.c` 的 `Modbus_DispatchPDU()` 里加一个 `case`，处理函数可以自己写（参考FC07/08两个自己实现的例子） |
| 官方点表跟这套设计思路差太多，不如重写 | 把 `modbus_data_map.h` 最上面 `#define MB_POINT_MAP_ENABLE` 注释掉，`modbus_data_map.c` 会切到 `#else` 分支的空壳+模板，Modbus从站框架照样工作，自己在5个函数里填代码 |
| 要求Modbus和自定义协议不能同时开 | `Protocol_mode` 强制成2或3就等于关掉了自定义协议的自动分流入口 |

---

## 九、赛前自检清单（⬜ 全是待办，必须在真机上做）

- [ ] 用 Modbus Poll（或同类工具）连上，默认参数（从站1、19200、8-N-1）下能读到 CH0/CH1 当前值
- [ ] FC03/FC04/FC05/FC06/FC16/FC07 各测一次，应答内容跟本手册第六节的期望值对得上
- [ ] RTU 和 ASCII 两种编码都测一遍（`Protocol_mode` 切2和3各测一次，或用自动嗅探模式分别发两种格式的帧）
- [ ] 写CH0变比、断电重启、再读回来，确认flash持久化生效
- [ ] Modbus主站持续轮询期间，确认自定义协议的定时自动上报确实被压住了，不会在RS485总线上跟主站轮询撞车（`Protocol_ModbusMasterActive()`生效验证）
- [ ] 从Modbus主站停止轮询、切回发一帧自定义协议帧，确认自动上报能自动恢复
- [ ] 官方点表下发后，改完 `modbus_data_map.h`，**同一份文件**在Keil和EIDE两边都要重新build一次（内容变了，但因为文件本身没有新增/删除，`test.uvprojx`和`eide.yml`都不用改注册，只是要记得两边都重新编译）
- [ ] 写一个不存在的地址，确认收到异常码 `0x02`（非法数据地址）而不是卡死或不回应
- [ ] 连续轮询几分钟，确认不掉线、不出现CRC错误累积

---

## 十、已知取舍

- **FC08诊断只实现了两个子功能**（0x0000回显、0x000A清计数器），vendor的`mbfuncdiag.c`本来就是空的license占位，其余子功能一律回非法功能码——官方如果考别的诊断子功能，要去`Protocol_Router.c`的`Modbus_FuncDiagnostic()`里加。
- **命令寄存器`MB_HREG_COMMAND`用魔术值而不是0/1**（重启=0x5201等），是故意的，防止主站顺手写个1就把设备重启。
- **T1.5帧内字符间隔检测没做**，只做T3.5帧间静默——跟自定义协议共用的那套TIM6判帧机制，单主站轮询总线上够用，多主站总线要重新评估。
- **决赛三路采样(i0_current/v0_voltage/v1_voltage/i0_broken)的地址不受点表总开关影响**——它们是`DeviceDataParams_t`结构体的成员，放这里是为了让ADC驱动不用加`extern`就能直接写，也是为了关掉点表时这几个决赛必考项仍然能编译过。
